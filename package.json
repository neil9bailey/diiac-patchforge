{
  "name": "diiac-patchforge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test backend-api/**/*.test.mjs",
    "test:contracts": "python -m pytest -q --basetemp .pytest_tmp",
    "collector:test": "node --test collector/*.test.mjs",
    "collector:build:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_patchforge_collector_windows_exe.ps1",
    "api:check": "node --check backend-api/server.js",
    "api:start": "node backend-api/server.js"
  }
}
